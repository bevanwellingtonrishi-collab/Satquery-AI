# AI Mode: "demo" (default, no API needed) or "live" (requires provider config)
AI_MODE=demo

# Optional: Live AI provider configuration
# Uncomment and configure to enable Live AI mode
# LIVE_AI_PROVIDER=gemini
# LIVE_AI_API_KEY=your_api_key_here
# LIVE_AI_MODEL=gemini-2.0-flash
